# 更新日志

所有重要的项目变更都会记录在这个文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 计划中的功能
- 更多自定义格式化选项
- 更好的错误提示

## [1.3.0] - 2025-12-30

### 重大优化

- 🚀 **性能优化**: 代码量减少 80%（从 1335 行优化到 ~260 行）
  - 采用 Token 化解析 + 文档重建的清晰架构
  - 消除大量重复代码和冗余方法
  - 简化逻辑流程，提升执行效率
  - 减少多次迭代处理，一次遍历完成

- 🏗️ **架构重构**:
  - 引入结构化的 Token 接口（`Token`、`ParsedAttribute`）
  - 统一的文档构建流程（`buildDocument`）
  - 清晰的职责分离（tokenize → build → restore）
  - 更好的类型安全和可维护性

### 修复

- 🐛 **多属性标签换行**: 修复属性超过 3 个时未正确换行的问题
  - 调整处理优先级，多属性检查优先于短文本处理
  - 支持占位符格式的属性解析（`__WXML_DIR_0__`）
  - 正确识别和格式化 `bind:tap` 等 WXML 指令

- 🐛 **短文本保持同行**: 修复 `<view>页面1</view>` 等短文本标签的格式化
  - 50 字符以内的文本内容保持在同一行
  - 内联标签（text、icon）始终保持同行
  - 优化标签内容的判断逻辑

### 技术改进

- ✨ **属性解析增强**: 支持占位符格式 `/([\w-:]+|__WXML_\w+_\d+__)=("[^"]*"|'[^']*')/g`
- 📝 **测试同步**: 更新测试文件使用新的格式化器实现
- 🧪 **测试覆盖**: 所有 12 个测试用例通过
- 📖 **代码质量**: 更清晰的代码结构和注释

### 性能对比

| 指标 | v1.2.4 | v1.3.0 | 改进 |
|------|--------|--------|------|
| 代码行数 | 1335 | ~260 | -80% |
| 方法数量 | 30+ | 10 | -67% |
| 迭代次数 | 最多 10 次 | 1 次 | -90% |
| 可维护性 | 中 | 高 | ↑ |

## [1.2.2] - 2025-07-21

### 修复

- 🐛 **重要修复**: 修复带连字符标签被截断的关键问题
  - **问题描述**: 格式化器无法正确处理微信小程序中的带连字符标签
  - **修复内容**:
    - 修复 `<scroll-view>` 被错误截断为 `<scroll>` 的问题
    - 修复 `<swiper-item>` 被错误截断为 `<swiper>` 的问题
    - 修复 `<picker-view>` 被错误截断为 `<picker>` 的问题
    - 修复 `<picker-view-column>` 被错误截断的问题
  - **影响范围**: 所有微信小程序带连字符标签，包括：
    - 布局组件：`scroll-view`, `swiper-item`, `movable-area`, `movable-view`
    - 表单组件：`picker-view`, `picker-view-column`
    - 媒体组件：`cover-view`, `cover-image`, `live-player`, `live-pusher`
    - 其他组件：`page-container`, `share-element`, `web-view`, `official-account`, `open-data`, `rich-text`

### 技术改进

- 🔧 **核心修复**: 修复了格式化器中12处正则表达式
  - 将 `\w+` 替换为 `[\w-]+` 以正确匹配带连字符的标签名
  - 涉及文件：`src/formatter.ts`
  - 修复位置：标签匹配、属性处理、格式化逻辑等关键部分
- ✅ **测试增强**: 新增专门的带连字符标签测试用例
  - 创建 `test/test-hyphenated-tags.js` 专项测试
  - 覆盖所有常用的带连字符标签
  - 确保修复效果和回归测试
- 📝 **文档完善**: 新增详细的修复文档
  - 创建 `bugLog/BUGFIX-HYPHENATED-TAGS.md` 详细记录
  - 包含问题分析、修复方案、测试验证等完整信息

### 质量保证

- 🎯 提升了对微信小程序标签的100%兼容性
- 🧪 增强了测试覆盖率，包含所有常用的带连字符标签
- 📖 完善了错误修复的文档记录和追踪机制
- 🔄 通过了完整的回归测试，确保不影响现有功能

## [1.2.0] - 2025-07-20

### 新增功能

- 🆕 **增强的格式化规则**:
  - 支持复杂嵌套的双花括号表达式处理
  - 可配置的属性换行阈值（默认3个属性）
  - 属性对齐和排序功能
  - 扩展的自闭合标签支持（video、audio、camera等）
  - 更多微信小程序组件支持

- ⚙️ **丰富的配置选项**:
  - `wrapAttributes`: 控制属性换行阈值
  - `alignAttributes`: 属性对齐开关
  - `sortAttributes`: 属性排序开关
  - `selfClosingTags`: 自定义自闭合标签列表
  - `inlineTags`: 自定义内联标签列表
  - `blockTags`: 自定义块级标签列表

- 📝 **测试增强**: 扩展的测试用例，包含更多复杂场景

### 改进优化

- 🔧 优化双花括号表达式保护机制
- 🎯 改进WXML指令和属性格式化
- 🚀 提升格式化性能和稳定性
- 📖 完善文档和配置说明

## [1.1.0] - 2025-07-20

### 新增功能

- 🆕 **智能格式化规则**:
  - `<text>` 标签内容保持在同一行，不进行换行
  - `<image>` 标签自动转换为自闭合格式 `<image ... />`
  - 属性超过3个时自动换行，提高可读性
  - 优化层级缩进，清晰展示标签嵌套关系

- 📝 **测试完善**: 完善的测试套件，包含多种格式化场景
- 📖 **文档增强**: 新增格式化规则说明文档

### 性能优化

- 🔧 优化格式化算法，提高处理复杂WXML结构的能力
- 🚀 提升格式化性能，处理速度约4500字符/ms
- 🎯 更精确的属性换行控制

## [1.0.0] - 2025-07-20

### 核心功能

- 🎉 **初始版本发布**
- 📝 **WXML格式化**: 支持WXML文件的完整格式化功能
- 🔧 **微信小程序语法支持**:
  - 条件渲染 (wx:if, wx:elif, wx:else)
  - 列表渲染 (wx:for, wx:for-item, wx:for-index, wx:key)
  - 事件绑定 (bind:, catch:, capture-bind:, capture-catch:)
  - 双花括号表达式 {{}}
  - 模板语法 (template, import, include)

### 用户体验

- 🎨 **语法高亮**: 完整的WXML语法高亮支持
- ⚙️ **可配置选项**:
  - 缩进大小
  - 最大行长度
  - 保留换行符设置
- 🚀 **多种格式化方式**:
  - 右键菜单格式化
  - 快捷键格式化
  - 命令面板格式化
  - 文档格式化和范围格式化

### 技术架构

- 🏗️ 基于 js-beautify 的HTML格式化引擎
- 🛡️ 智能保护WXML表达式不被破坏
- 📘 TypeScript 开发，类型安全
- 🔌 完整的VSCode扩展API集成
